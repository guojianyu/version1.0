

该目录下的文件与执行器的脚本存储，调用方式，执行方式相关

目录结构：(重要的文件和文件夹)

                    |---------collection_doc(该目录下存放所有的执行脚本)
                    |
                    |
excutor_process-----|
                    |
                    |
                    |---------scan_process（该文件是负责开启执行的进程和线程相关的逻辑）



整体说明：

整体实现思路：

    为了减少各个脚本获取任务都要与中间层通信的缺点，而获取任务的这个任务任务单独交与独立进程进行，从而减少了连接数的问题，从而也可以统一管理。
获取任务的进程采用轮询任务类型的方式获取各种任务，但是盛放任务的队列只有一个。负责调用脚本的线程将从队列中获取任务，根据任务类型调用不同的脚本。

一：关于进程的说明：
   1：进程的分类：（1）与中间层通信进程（2）调用脚本进程 （（3）抓取器进程，目前没有）
   2：个进程的详细说明：与中间层通信进程采用的是进程，进程数默认很小，它并不是耗时操作。
                      调用脚本进程采用的是开启进程，然后进程开启线程，真正意义上负责调用脚本的是线程。
                      （抓取器进程，判断任务属性决定是否使用异步与何种抓取模块）

