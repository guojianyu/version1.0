#服务器会有一个周期性任务扫描任务队列，将未加入工作队列的任务加入工作队列，它的执行时间是time+timeout ，到了超时时间会检查任务的属性状态，
如果任务状态还是0或1，调用执行器将任务添加到超时队列。如果状态大于1则认为该任务被分配到了客户端，调用的执行器，根据不同的任务类型做不同的
操作，如果任务状态为2正在执行，应该删除当前任务，生成一个增加时间的新任务，其他的任务状态根据任务属性不同进行删除或其他操作。



为了使任务属性的时间与apscheduler的时间一致，都采用datetime 时间


细节部分，服务器在删除总任务时应该记录，告知客户端删除

ERROR.txt是记录错误日志的



关于apscheduler_server 目录结构说明：


1:zmq_interface #该目录下的interface.py用zmq实现的与服务器通信的接口（可被web替换）

2：apscheduler_server #任务调度中心服务器功能目录

                  |------------aioweb.py 任务调度中心服务器和客户端的通信接口（web实现的）
                  |
                  |------------aps_server.py 任务就绪，任务超时的逻辑部分
apscheduler_server|
                  |------------获取代理（预留）
                  |
                  |------------获取 cookie（预留）
                  |
                  |------------other